# Usuarios

CRUD con búsqueda, filtros por estado/rol, asignación de roles y activación/desactivación.

Permisos: `users.view`, `users.create`, `users.edit`, `users.delete`.

Rutas bajo `/users`. El registro público crea cuentas en estado `pending` sin roles.
